#ifndef SECRET_REWARD_H_
#define SECRET_REWARD_H_

#include "../common.h"

#define SR_SIZE 8

void init_secret_reward(char x, char y);
void draw_secret_reward(char ix);
void update_secret_reward(char ix);

#endif // SECRET_REWARD_H_
